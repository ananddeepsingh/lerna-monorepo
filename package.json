{
  "name": "root",
  "version": "1.0.0",
  "devDependencies": {
    "lerna": "^7.2.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "lerna run start",
    "test": "lerna run test",
    "diff": "lerna diff"
  }
}